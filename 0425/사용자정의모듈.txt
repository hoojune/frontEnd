사용자정의 모듈은 

인트로로 들어가면 인트로에서 화면이동을 한다.
만들어놓은 관리모듈로 이동한다면 관리모듈의 라우터로 
다른앱으로 이동한다.

관리모듈에서는 각앱의 라우터를 만들어 설정해줘야한다.
관리모듈은 보통 router폴더에 있으면
각각의 라우터만 관리하는 json파일을 만든다.

view파일에 index,html을 만들어 연결해준다.

data파일에 view파일에서 처리되어진 결과물,자료을 가져온다,

